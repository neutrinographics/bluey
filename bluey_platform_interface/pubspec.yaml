name: bluey_platform_interface
description: Platform interface for Bluey - defines the contract that platform implementations must follow
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.7.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.11.0
  plugin_platform_interface: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
