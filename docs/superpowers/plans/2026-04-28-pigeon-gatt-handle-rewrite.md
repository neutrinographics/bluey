# Bundled Architectural Rewrite — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement the bundled rewrite of I088 (Pigeon GATT handle identity), I089/I066 (Android-tagged Connection extensions), I300 (PeerConnection composition), and I301 (value objects for Mtu / ConnectionParameters / handles). Major-version-bump breaking change; only the example app consumes Bluey, so no migration guide.

**Architecture:** Five phases — value objects → platform-tagged extensions → peer composition → Pigeon handle rewrite → cleanup. Each phase ends at a review checkpoint where the suite is green, the example app builds, and the user manually verifies. Phase D (handle rewrite) is sequenced last so the handle work doesn't have to keep changing as Connection's surface settles.

**Tech Stack:** Dart 3, Flutter, Pigeon, Kotlin (Android), Swift (iOS), JVM unit tests (Robolectric / mockk), XCTest, FakeBlueyPlatform.

**Spec:** [`docs/superpowers/specs/2026-04-28-pigeon-gatt-handle-rewrite-design.md`](../specs/2026-04-28-pigeon-gatt-handle-rewrite-design.md).

**Working directory for all commands:** `/Users/joel/git/neutrinographics/bluey`.

**Branch:** create and execute on `feat/handle-identity-rewrite` off `main`.

**Bundled backlog IDs (closed at end of plan):** I010, I011, I016, I066, I088, I089, I300, I301.

---

## File Structure

| File | Role |
|---|---|
| `bluey/lib/src/connection/value_objects/attribute_handle.dart` | NEW: opaque handle wrapper |
| `bluey/lib/src/connection/value_objects/mtu.dart` | NEW: Mtu value object |
| `bluey/lib/src/connection/value_objects/connection_interval.dart` | NEW |
| `bluey/lib/src/connection/value_objects/peripheral_latency.dart` | NEW |
| `bluey/lib/src/connection/value_objects/supervision_timeout.dart` | NEW |
| `bluey/lib/src/connection/value_objects/connection_parameters.dart` | NEW: moves out of `connection.dart` |
| `bluey/lib/src/connection/connection_parameters_mapper.dart` | NEW: domain ↔ platform-interface conversion |
| `bluey/lib/src/connection/android_connection_extensions.dart` | NEW |
| `bluey/lib/src/connection/ios_connection_extensions.dart` | NEW (empty placeholder) |
| `bluey/lib/src/connection/connection.dart` | Modify: remove bond/PHY/conn-params, isBlueyServer/serverId, add android/ios accessors, change mtu type, narrow service/characteristic/descriptor singular accessors |
| `bluey/lib/src/connection/bluey_connection.dart` | Modify: remove _lifecycle/_serverId/upgrade/control-service-filter, add extension impls, add `AttributeHandle` to remote types, add plural accessors, throw on ambiguity |
| `bluey/lib/src/peer/peer_connection.dart` | NEW: `PeerConnection` interface + `_BlueyPeerConnection` impl |
| `bluey/lib/src/peer/peer_remote_service_view.dart` | NEW: control-service-hiding view |
| `bluey/lib/src/peer/bluey_peer.dart` | Modify: `connect()` returns `PeerConnection` |
| `bluey/lib/src/bluey.dart` | Modify: `connect()` returns raw, add `connectAsPeer` / `tryUpgrade`, rename `_upgradeIfBlueyServer` → `_tryBuildPeerConnection` |
| `bluey/lib/src/exceptions.dart` | Modify: add `NotABlueyPeerException`, `AttributeHandleInvalidatedException`, `AttributeNotFoundException`, `AmbiguousAttributeException` |
| `bluey/lib/bluey.dart` (barrel) | Modify: re-export new value objects + extension types |
| `bluey_platform_interface/lib/src/platform_interface.dart` | Modify: add `handle` to `PlatformCharacteristic`/`PlatformDescriptor`/request DTOs; switch GATT methods to handle params |
| `bluey_android/pigeons/messages.dart` | Modify: handle field on DTOs, handle param on GATT methods |
| `bluey_ios/pigeons/messages.dart` | Modify: same |
| `bluey_android/lib/src/messages.g.dart` | Regenerated by pigeon |
| `bluey_android/android/src/main/kotlin/com/neutrinographics/bluey/Messages.g.kt` | Regenerated |
| `bluey_ios/lib/src/messages.g.dart` | Regenerated |
| `bluey_ios/ios/Classes/Messages.g.swift` | Regenerated |
| `bluey_android/android/src/main/kotlin/com/neutrinographics/bluey/ConnectionManager.kt` | Modify: handle tables, replace `findCharacteristic`/`findDescriptor` |
| `bluey_android/android/src/main/kotlin/com/neutrinographics/bluey/GattServer.kt` (or wherever) | Modify: emit `getInstanceId()` in DTOs, route by handle |
| `bluey_ios/ios/Classes/CentralManagerImpl.swift` | Modify: handle tables, mint handles, lookup by handle |
| `bluey_ios/ios/Classes/PeripheralManagerImpl.swift` | Modify: handle table, mint at `addService`, lookup by handle |
| `bluey/test/fakes/fake_platform.dart` | Modify: handle-keyed storage, `handleFor()` helper |
| `bluey/test/fakes/test_helpers.dart` | Modify: add `TestHandles` |
| `bluey/test/connection/value_objects/*_test.dart` | NEW: Mtu / interval / latency / timeout / handle / params tests |
| `bluey/test/connection/handle_routing_test.dart` | NEW |
| `bluey/test/connection/cccd_routing_test.dart` | NEW |
| `bluey/test/connection/ambiguous_attribute_test.dart` | NEW |
| `bluey/test/connection/android_extensions_test.dart` | NEW |
| `bluey/test/peer/peer_connection_test.dart` | NEW |
| `bluey/test/peer/try_upgrade_test.dart` | NEW |
| `bluey_android/android/src/test/kotlin/.../HandleLookupTest.kt` | NEW |
| `bluey_android/android/src/test/kotlin/.../ServiceChangedHandleTest.kt` | NEW |
| `bluey_ios/ios/Tests/CentralManagerHandleTests.swift` | NEW |
| `bluey_ios/ios/Tests/PeripheralManagerHandleTests.swift` | NEW |
| `bluey/example/lib/...` | Modify: migrate to value objects, `connection.android?`, `connectAsPeer` |
| `bluey/CHANGELOG.md` and siblings | Modify: major-version notes |
| `bluey/pubspec.yaml` and siblings | Modify: bump major version |
| `bluey_android/ANDROID_BLE_NOTES.md` | Modify: handle-lifetime invariant |
| `bluey_ios/IOS_BLE_NOTES.md` | Modify: handle-minting note |
| `CLAUDE.md` | Modify: handle-identity invariant + connection.android pattern |
| `docs/backlog/I010.md`, `I011.md`, `I016.md`, `I066.md`, `I088.md`, `I089.md`, `I300.md`, `I301.md` | Modify: `status: fixed`, `fixed_in: <sha>` |
| `docs/backlog/README.md` | Modify: move bundle to "Fixed — verified in HEAD" |

---

## Pre-flight: Branch and baseline

- [ ] **P0.1 — Create the feature branch from a clean main.**
  ```bash
  git checkout main
  git pull --ff-only
  git status   # clean working tree expected
  git checkout -b feat/handle-identity-rewrite
  ```
- [ ] **P0.2 — Capture baseline test counts** so regressions are caught later.
  ```bash
  cd bluey && flutter test 2>&1 | tail -3
  cd ../bluey_platform_interface && flutter test 2>&1 | tail -3
  cd ../bluey_android && flutter test 2>&1 | tail -3 || true
  cd ../bluey_ios && flutter test 2>&1 | tail -3 || true
  cd ..
  ```
  Record the numbers in a scratch note. Expected baseline (from spec): bluey 653, bluey_platform_interface 32; Android JVM and iOS XCTest counts captured separately if available.

- [ ] **P0.3 — Confirm `flutter analyze` is clean across all four packages.**
  ```bash
  cd bluey && flutter analyze
  cd ../bluey_platform_interface && flutter analyze
  cd ../bluey_android && flutter analyze
  cd ../bluey_ios && flutter analyze
  cd ..
  ```
  Any pre-existing warnings: note them so you don't conflate with new ones.

---

# Phase A — Value objects (I301 + AttributeHandle)

**Goal:** Introduce `AttributeHandle`, `Mtu`, `ConnectionInterval`, `PeripheralLatency`, `SupervisionTimeout`, value-object `ConnectionParameters`, and the domain ↔ platform-interface mapper. Change `Connection.mtu` from `int` to `Mtu` and `Connection.requestMtu` to take `Mtu`.

**Phase A checkpoint:** all tests green, example app builds, `connection.requestMtu(Mtu(...))` is the only way to set MTU.

## Task A.1: Add `AttributeHandle` value object (Red→Green)

**Rationale:** Foundation for Phase D. Adding it first means we can reference `AttributeHandle` in later phases without re-touching Phase A. The class is simple but earns its keep — wraps wire-level int with `value > 0` validation and equality-by-value.

**Files:**
- New: `bluey/lib/src/connection/value_objects/attribute_handle.dart`
- New: `bluey/test/connection/value_objects/attribute_handle_test.dart`
- Modify: `bluey/lib/bluey.dart` (re-export)

- [ ] **A.1.1** — Write failing tests in `attribute_handle_test.dart` covering:
  - `AttributeHandle(1)` constructs successfully.
  - `AttributeHandle(0)` throws `ArgumentError`.
  - `AttributeHandle(-5)` throws `ArgumentError`.
  - Equality: `AttributeHandle(7) == AttributeHandle(7)`, `hashCode` matches.
  - `AttributeHandle(7) != AttributeHandle(8)`.
  - `toString()` includes the value.
  Run `flutter test test/connection/value_objects/attribute_handle_test.dart`. Expect: file-not-found / class-not-found compile error.
- [ ] **A.1.2** — Implement `AttributeHandle` per the spec (`@immutable`, `final int value`, throwing constructor, equality, hashCode, toString).
- [ ] **A.1.3** — Re-export from `bluey/lib/bluey.dart`.
- [ ] **A.1.4** — Run the test file → green.
- [ ] **A.1.5** — Commit:
  ```
  feat(bluey): add AttributeHandle value object (I088 prep)
  ```

## Task A.2: Add `Mtu` value object

**Files:**
- New: `bluey/lib/src/connection/value_objects/mtu.dart`
- New: `bluey/test/connection/value_objects/mtu_test.dart`
- Modify: `bluey/lib/bluey.dart` (re-export)

- [ ] **A.2.1** — Write failing tests:
  - `Mtu(23, capabilities: androidCaps)` succeeds; `.value == 23`.
  - `Mtu(517, capabilities: androidCaps)` succeeds where `androidCaps.maxMtu == 517`.
  - `Mtu(518, capabilities: androidCaps)` throws.
  - `Mtu(185, capabilities: iosCaps)` succeeds where `iosCaps.maxMtu == 185`.
  - `Mtu(186, capabilities: iosCaps)` throws.
  - `Mtu(22, capabilities: anyCaps)` throws (below BLE minimum).
  - `Mtu.minimum.value == 23`.
  - `Mtu.fromPlatform(517)` succeeds without capabilities (platform is authoritative).
  - Equality / hashCode by value.
- [ ] **A.2.2** — Implement `Mtu` per spec.
- [ ] **A.2.3** — Re-export from barrel; run tests → green.
- [ ] **A.2.4** — Commit:
  ```
  feat(bluey): add Mtu value object with Capabilities-aware bounds (I301)
  ```

## Task A.3: Add `ConnectionInterval`, `PeripheralLatency`, `SupervisionTimeout`, value-object `ConnectionParameters`

**Files:**
- New: `bluey/lib/src/connection/value_objects/connection_interval.dart`
- New: `bluey/lib/src/connection/value_objects/peripheral_latency.dart`
- New: `bluey/lib/src/connection/value_objects/supervision_timeout.dart`
- New: `bluey/lib/src/connection/value_objects/connection_parameters.dart`
- New: `bluey/test/connection/value_objects/connection_parameters_test.dart`
- Modify: `bluey/lib/bluey.dart`

- [ ] **A.3.1** — Failing tests for boundary values on each scalar VO (7.5/4000 ms; 0/499; 100/32000 ms) and the cross-field invariant on `ConnectionParameters` (`timeout > (1 + latency) * interval`).
- [ ] **A.3.2** — Implement the four classes per spec.
- [ ] **A.3.3** — Run → green.
- [ ] **A.3.4** — Commit:
  ```
  feat(bluey): add ConnectionInterval/PeripheralLatency/SupervisionTimeout VOs and value-object ConnectionParameters (I301)
  ```

## Task A.4: Mapper between domain `ConnectionParameters` and `PlatformConnectionParameters`

**Rationale:** Decision 3 in spec — keep domain pure of platform-interface dependencies. Mapper lives in `bluey/lib/src/connection/`, takes a domain VO and returns the platform DTO (and vice-versa for reads).

**Files:**
- New: `bluey/lib/src/connection/connection_parameters_mapper.dart`
- New: `bluey/test/connection/connection_parameters_mapper_test.dart`

- [ ] **A.4.1** — Failing test: `mapper.toPlatform(domainParams).intervalMs == domainParams.interval.milliseconds`. Round-trip: `mapper.fromPlatform(mapper.toPlatform(p)) == p`.
- [ ] **A.4.2** — Implement two top-level functions or a class with two static methods.
- [ ] **A.4.3** — Run → green.
- [ ] **A.4.4** — Commit:
  ```
  feat(bluey): add ConnectionParameters mapper (domain ↔ wire) (I301)
  ```

## Task A.5: Switch `Connection.mtu` and `Connection.requestMtu` to use `Mtu`

**Rationale:** Breaking interface change but isolated. After this task, all callers pass `Mtu` and read `Mtu`.

**Files:**
- Modify: `bluey/lib/src/connection/connection.dart` (L195 `requestMtu`; mtu getter at L163)
- Modify: `bluey/lib/src/connection/bluey_connection.dart` (`_mtu` field at L167, `mtu` getter at L364, `requestMtu` impl)
- Modify: `bluey/test/fakes/fake_platform.dart` (switch fake `_mtu` to int internally; expose Mtu at boundary)
- Modify: every test that calls `connection.requestMtu(int)` or asserts on `connection.mtu` as int.
- Modify: `bluey/example/lib/...` MTU call sites.

- [ ] **A.5.1** — In `connection.dart`, change:
  - `int get mtu` → `Mtu get mtu`
  - `Future<int> requestMtu(int mtu)` → `Future<Mtu> requestMtu(Mtu mtu)`
- [ ] **A.5.2** — In `bluey_connection.dart`:
  - Keep `_mtu: int` internally (it's the negotiated value from the platform).
  - Change `mtu` getter to `Mtu.fromPlatform(_mtu)`.
  - Change `requestMtu(Mtu mtu)` to forward `mtu.value` to `_platform.requestMtu(deviceId, mtu.value)`, then return `Mtu.fromPlatform(_mtu)` after the negotiation completes.
- [ ] **A.5.3** — Run analyzer to find all call sites:
  ```bash
  cd bluey && flutter analyze 2>&1 | head -100
  ```
  Update each.
- [ ] **A.5.4** — Update FakeBlueyPlatform if it needs to expose typed Mtu.
- [ ] **A.5.5** — Migrate example app:
  ```bash
  cd bluey/example && flutter analyze
  ```
  Update any `requestMtu(int)` calls.
- [ ] **A.5.6** — Run full suite:
  ```bash
  cd bluey && flutter test
  cd ../bluey_platform_interface && flutter test
  ```
  Expect: all green (with call-site updates only; no semantic test changes).
- [ ] **A.5.7** — Commit:
  ```
  refactor(bluey): Connection.mtu and requestMtu now take/return Mtu value object (I301, breaking)
  ```

## Phase A checkpoint

- [ ] **A.checkpoint.1** — `flutter test` green for `bluey` and `bluey_platform_interface`. (Android/iOS untouched in Phase A.)
- [ ] **A.checkpoint.2** — `flutter analyze` clean across all four packages.
- [ ] **A.checkpoint.3** — Example app builds and the connect → MTU flow works manually on a real device (smoke test only — full Phase D verification is later).
- [ ] **A.checkpoint.4** — Pause for user review before starting Phase B.

---

# Phase B — Platform-tagged Connection extensions (I089/I066)

**Goal:** Move bond / PHY / connection-parameters / connection-priority / refreshGattCache off `Connection` and onto `connection.android: AndroidConnectionExtensions?`. Add typed-null `connection.ios: IosConnectionExtensions?` (empty, reserved). Capabilities matrix decides whether `connection.android` is non-null.

**Phase B checkpoint:** breaking; every existing call to `connection.bond()` etc. has migrated to `connection.android?.bond()`.

## Task B.1: Introduce abstract `AndroidConnectionExtensions` and empty `IosConnectionExtensions`

**Files:**
- New: `bluey/lib/src/connection/android_connection_extensions.dart`
- New: `bluey/lib/src/connection/ios_connection_extensions.dart`
- Modify: `bluey/lib/bluey.dart` (re-export both)

- [ ] **B.1.1** — Write `AndroidConnectionExtensions` per spec. Declare exactly the 10 members that currently exist on the `Connection` interface: bondState, bondStateChanges, bond, removeBond, txPhy, rxPhy, phyChanges, requestPhy, connectionParameters, requestConnectionParameters. The spec's forward-looking `requestConnectionPriority` and `refreshGattCache` are NOT included — they don't exist on `Connection` today, and adding net-new API in a "relocate existing surface" task would be scope creep. They can be added later when their underlying platform plumbing lands (I033 et al).
- [ ] **B.1.2** — Write `IosConnectionExtensions` (empty body, doc-comment "reserved for future iOS-specific features").
- [ ] **B.1.3** — Re-export both from barrel.
- [ ] **B.1.4** — Run `flutter analyze` → clean (no impl yet, just type declarations).
- [ ] **B.1.5** — Commit:
  ```
  refactor(bluey): introduce AndroidConnectionExtensions and IosConnectionExtensions interfaces (I089)
  ```

## Task B.2: Wire `connection.android` / `connection.ios` accessors gated by Capabilities

**Rationale:** `BlueyConnection` exposes the extension instance only when capabilities permit. Android-flavored capabilities → non-null android extension; iOS-flavored → null android, non-null ios singleton.

**Files:**
- Modify: `bluey/lib/src/connection/connection.dart` — add `android: AndroidConnectionExtensions?` and `ios: IosConnectionExtensions?` getters.
- Modify: `bluey/lib/src/connection/bluey_connection.dart` — implement getters; add private inner classes `_AndroidConnectionExtensionsImpl` and `_IosConnectionExtensionsImpl`.
- New: `bluey/test/connection/android_extensions_test.dart`

- [ ] **B.2.1** — Failing tests:
  - With Android capabilities, `connection.android` is non-null and routes `bond()` etc. through `_platform`.
  - With iOS capabilities, `connection.android` is null.
  - With Android capabilities, `connection.ios` is null.
  - With iOS capabilities, `connection.ios` is the (empty) singleton.
- [ ] **B.2.2** — Add `android` and `ios` abstract getters to `Connection`.
- [ ] **B.2.3** — Implement on `BlueyConnection`:
  - `android` returns `_androidExt` (lazy-initialized once if `capabilities.canBond || canRequestPhy || canRequestConnectionParameters`, else null).
  - `ios` returns `_iosExt` singleton on iOS (gate on a capability or a platform check; defaulting to non-null on iOS-flavored capabilities is fine).
- [ ] **B.2.4** — Inner class `_AndroidConnectionExtensionsImpl` delegates each method to the existing `_platform.bond(deviceId)` etc. (Today these methods exist on the platform interface; we're routing them through the extension class instead of the Connection interface.)
- [ ] **B.2.5** — Update FakeBlueyPlatform (or the test helper that constructs `BlueyConnection`) to allow tests to flip capabilities.
- [ ] **B.2.6** — Run target test → green.
- [ ] **B.2.7** — Commit:
  ```
  feat(bluey): connection.android / connection.ios accessors gated by Capabilities (I089)
  ```

## Task B.3: Remove bond/PHY/conn-params from `Connection` (breaking)

**Rationale:** Now that `connection.android?.bond()` works, the cross-platform Connection interface drops the asymmetric methods. This is the breaking step.

**Files:**
- Modify: `bluey/lib/src/connection/connection.dart` — delete the bond / PHY / connection-parameters sections (L211–290).
- Modify: `bluey/lib/src/connection/bluey_connection.dart` — delete the corresponding implementations (move their bodies into the `_AndroidConnectionExtensionsImpl` if not already there).
- Modify: every call site in `bluey/test/`, `bluey/example/`, internal `bluey/lib/`.

- [ ] **B.3.1** — Delete the abstract declarations from `Connection`.
- [ ] **B.3.2** — Move (don't duplicate) the impls into `_AndroidConnectionExtensionsImpl` if they aren't already there.
- [ ] **B.3.3** — Run analyzer:
  ```bash
  cd bluey && flutter analyze
  ```
  Walk every reported call site that says "method not defined on Connection". Migrate to `connection.android?.method()`. Note: `connection.android?.bond()` returns `Future<void>?` — null-aware. Callers that need to treat null-return as success can use `?? Future.value()`.
- [ ] **B.3.4** — Repeat for example app:
  ```bash
  cd bluey/example && flutter analyze
  ```
- [ ] **B.3.5** — Run full suite for `bluey` and example app.
- [ ] **B.3.6** — Commit:
  ```
  refactor(bluey): remove bond/PHY/conn-params from Connection; live on connection.android? (I089, breaking)
  ```

## Task B.4: Migrate example app to `connection.android?` pattern

**Rationale:** May overlap with B.3 if call sites in example app surfaced earlier; this task is a finishing pass to ensure example builds cleanly.

**Files:** `bluey/example/lib/...`

- [ ] **B.4.1** — Run `cd bluey/example && flutter analyze` → expect clean.
- [ ] **B.4.2** — Run `cd bluey/example && flutter run` on a device or simulator. Smoke-test connect + (if Android) bond.
- [ ] **B.4.3** — Commit (only if there are changes beyond B.3):
  ```
  refactor(example): migrate bond/PHY/conn-params to connection.android? (I089)
  ```

## Phase B checkpoint

- [ ] **B.checkpoint.1** — `flutter test` green for `bluey` and `bluey_platform_interface`.
- [ ] **B.checkpoint.2** — `flutter analyze` clean across all packages.
- [ ] **B.checkpoint.3** — Example app builds; on Android, `connection.android?.bond()` reaches the existing stub. On iOS, `connection.android` is null.
- [ ] **B.checkpoint.4** — Pause for user review before Phase C.

---

# Phase C — PeerConnection composition (I300)

**Goal:** Extract Peer-protocol concerns from `Connection`. Introduce `PeerConnection` as a composition wrapper. `Bluey.connect()` returns raw `Connection`; new `Bluey.connectAsPeer(device)` returns `PeerConnection` (throws on miss); new `Bluey.tryUpgrade(connection)` returns `PeerConnection?`. Control-service filtering moves into `PeerRemoteServiceView`. `BlueyConnection.upgrade()`, `isBlueyServer`, `serverId` are removed.

## Task C.1: Introduce `PeerConnection` interface and `_BlueyPeerConnection` impl

**Files:**
- New: `bluey/lib/src/peer/peer_connection.dart`
- New: `bluey/test/peer/peer_connection_test.dart` (tests added incrementally)
- Modify: `bluey/lib/bluey.dart` (re-export)

- [ ] **C.1.1** — Failing test: a `_BlueyPeerConnection` constructed with a fake Connection + ServerId + LifecycleClient stub exposes `connection`, `serverId`, and forwards `disconnect()` to the wrapped Connection.
- [ ] **C.1.2** — Define the `PeerConnection` abstract class (interface + impl per spec).
- [ ] **C.1.3** — Re-export from barrel.
- [ ] **C.1.4** — Run target test → green.
- [ ] **C.1.5** — Commit:
  ```
  feat(bluey/peer): introduce PeerConnection interface and _BlueyPeerConnection impl (I300)
  ```

## Task C.2: Introduce `PeerRemoteServiceView`

**Rationale:** Today's control-service filtering lives in `BlueyConnection.service/services/hasService`. Move it into a view layer that wraps `Connection.services()` and excludes the lifecycle control service for peer-protocol consumers.

**Files:**
- New: `bluey/lib/src/peer/peer_remote_service_view.dart`
- New: `bluey/test/peer/peer_remote_service_view_test.dart`

- [ ] **C.2.1** — Failing test: a view wrapping a Connection that exposes [controlService, userService] returns only [userService] from `services()`; `service(controlServiceUuid)` throws `ServiceNotFoundException`.
- [ ] **C.2.2** — Implement `PeerRemoteServiceView` as a thin wrapper.
- [ ] **C.2.3** — Run target test → green.
- [ ] **C.2.4** — Commit:
  ```
  feat(bluey/peer): introduce PeerRemoteServiceView (control-service hider) (I300)
  ```

## Task C.3: Wire `_BlueyPeerConnection` to use `PeerRemoteServiceView`

**Files:** `bluey/lib/src/peer/peer_connection.dart`

- [ ] **C.3.1** — Failing test: `peer.services()` excludes control service even though `peer.connection.services()` includes it (raw Connection is unfiltered).
- [ ] **C.3.2** — Wire `_BlueyPeerConnection.services / service / hasService` to delegate through a private `PeerRemoteServiceView`.
- [ ] **C.3.3** — Run → green.
- [ ] **C.3.4** — Commit:
  ```
  refactor(bluey/peer): _BlueyPeerConnection delegates service tree through PeerRemoteServiceView (I300)
  ```

## Task C.4: Add `Bluey.connectAsPeer` and `Bluey.tryUpgrade`

**Rationale:** New entry points that operate alongside existing `Bluey.connect`. The existing `_upgradeIfBlueyServer` becomes `_tryBuildPeerConnection(Connection): Future<PeerConnection?>`.

**Files:**
- Modify: `bluey/lib/src/bluey.dart`
- Modify: `bluey/lib/src/exceptions.dart` — add `NotABlueyPeerException`
- New: `bluey/test/peer/try_upgrade_test.dart`

- [ ] **C.4.1** — Failing tests:
  - `bluey.connectAsPeer(peerDevice)` returns a `PeerConnection` with the correct `serverId`.
  - `bluey.connectAsPeer(non-peer-device)` throws `NotABlueyPeerException`.
  - `bluey.tryUpgrade(non-peer-connection)` returns null.
  - `bluey.tryUpgrade(peer-connection)` returns the upgraded peer.
- [ ] **C.4.2** — Add `NotABlueyPeerException`.
- [ ] **C.4.3** — Rename `_upgradeIfBlueyServer` → `_tryBuildPeerConnection`. Change return type from `Future<Connection>` (mutated raw) to `Future<PeerConnection?>` (fresh wrapper). Body: same control-service detection; on success, build `_BlueyPeerConnection(connection, serverId, lifecycleClient)`; on miss, return null.
- [ ] **C.4.4** — Add `Bluey.connectAsPeer(device)`: calls `connect`, then `_tryBuildPeerConnection`; throws on null.
- [ ] **C.4.5** — Add `Bluey.tryUpgrade(connection)`: just calls `_tryBuildPeerConnection`.
- [ ] **C.4.6** — Run target tests → green.
- [ ] **C.4.7** — Commit:
  ```
  feat(bluey): add connectAsPeer and tryUpgrade APIs (I300)
  ```

## Task C.5: `Bluey.connect()` no longer auto-upgrades

**Rationale:** This is the breaking change. `Bluey.connect(device)` now returns raw `Connection` always. Callers that expected `connection.isBlueyServer == true` migrate to `bluey.connectAsPeer(device)`.

**Files:**
- Modify: `bluey/lib/src/bluey.dart`
- Modify: `bluey/test/` — anywhere that asserts `connection.isBlueyServer` migrates to `connectAsPeer`.

- [ ] **C.5.1** — In `Bluey.connect`, remove the `_upgradeIfBlueyServer` call. The path is now: connect → return raw `Connection`.
- [ ] **C.5.2** — Run analyzer; walk failures.
- [ ] **C.5.3** — Migrate test sites: tests that did `final c = await bluey.connect(d); expect(c.isBlueyServer, true)` become `final p = await bluey.connectAsPeer(d); expect(p.serverId, isNotNull)`.
- [ ] **C.5.4** — Migrate `bluey/example/lib/` similarly.
- [ ] **C.5.5** — Run full suite → green.
- [ ] **C.5.6** — Commit:
  ```
  refactor(bluey): Bluey.connect returns raw Connection; peer upgrade is explicit (I300, breaking)
  ```

## Task C.6: Remove `BlueyConnection.upgrade`, `isBlueyServer`, `serverId`, control-service filtering

**Rationale:** With nothing calling them, delete.

**Files:**
- Modify: `bluey/lib/src/connection/connection.dart` — delete `isBlueyServer` (L144), `serverId` (L148).
- Modify: `bluey/lib/src/connection/bluey_connection.dart` — delete `_lifecycle` (L153), `_serverId` (L154), `isBlueyServer` (L157), `serverId` (L160), `upgrade()` (L296–315), control-service filtering (L368–369 and parallel sites in `services`/`hasService`).

- [ ] **C.6.1** — Run analyzer to confirm zero references.
- [ ] **C.6.2** — Delete the members.
- [ ] **C.6.3** — Run full suite → green.
- [ ] **C.6.4** — Commit:
  ```
  refactor(bluey): remove BlueyConnection.upgrade / isBlueyServer / serverId / control-service filter (I300)
  ```

## Task C.7: `BlueyPeer.connect` returns `PeerConnection`

**Files:**
- Modify: `bluey/lib/src/peer/bluey_peer.dart`
- Modify: dependent tests / example app.

- [ ] **C.7.1** — Failing test: `BlueyPeer(...).connect()` returns a `PeerConnection`, not a raw `Connection`.
- [ ] **C.7.2** — Change return type and inner impl. The path is `bluey.connectAsPeer(device)` under the hood.
- [ ] **C.7.3** — Migrate call sites.
- [ ] **C.7.4** — Run full suite → green.
- [ ] **C.7.5** — Commit:
  ```
  refactor(bluey/peer): BlueyPeer.connect returns PeerConnection (I300, breaking)
  ```

## Phase C checkpoint

- [ ] **C.checkpoint.1** — `flutter test` green for `bluey` and `bluey_platform_interface`.
- [ ] **C.checkpoint.2** — `flutter analyze` clean.
- [ ] **C.checkpoint.3** — Example app builds. `bluey.connect()` returns raw; `bluey.connectAsPeer()` returns peer.
- [ ] **C.checkpoint.4** — Manual: connect to a Bluey peer via `connectAsPeer`; serverId is correct; control service is hidden in `peer.services()` but visible in `peer.connection.services()`.
- [ ] **C.checkpoint.5** — Pause for user review before Phase D.

---

# Phase D — Pigeon GATT handle rewrite (I088)

**Goal:** Replace UUID-keyed GATT routing with handle-keyed routing on both platforms. Pigeon schema additive first (UUID + handle), then breaking (handle-only). Domain layer carries `AttributeHandle` on `BlueyRemoteCharacteristic` / `BlueyRemoteDescriptor` and unwraps to int at the Pigeon boundary. Plural accessors and ambiguity-throw added.

This phase is sequenced platform-by-platform inside the additive period, then a single breaking-cleanup commit drops UUIDs.

## Task D.1: Pigeon — add `handle` field to DTOs and request DTOs (additive, both platforms)

**Files:**
- Modify: `bluey_android/pigeons/messages.dart` — add `final int handle` to `CharacteristicDto` (L103–113), `DescriptorDto` (L96–100), `ReadRequestDto` (L253–265), `WriteRequestDto` (L268–284). Make the field nullable in this commit so non-handle-aware native sides still compile (we'll tighten later).
- Modify: `bluey_ios/pigeons/messages.dart` — same fields.
- Regenerate bindings:
  ```bash
  cd bluey_android && dart run pigeon --input pigeons/messages.dart
  cd ../bluey_ios && dart run pigeon --input pigeons/messages.dart
  ```

- [ ] **D.1.1** — Edit both pigeon files.
- [ ] **D.1.2** — Regenerate bindings.
- [ ] **D.1.3** — Confirm `flutter analyze` clean across affected packages.
- [ ] **D.1.4** — Commit:
  ```
  refactor(pigeons): add nullable handle field to DTOs and request DTOs (additive, I088 prep)
  ```

## Task D.2: Platform interface — add `handle` to `PlatformCharacteristic` / `PlatformDescriptor` / request DTOs

**Files:**
- Modify: `bluey_platform_interface/lib/src/platform_interface.dart` — `PlatformDescriptor` (L127), `PlatformCharacteristic` (L135), `PlatformReadRequest` (L671), `PlatformWriteRequest` (L687).
- Modify: `bluey_android/lib/bluey_android.dart` and `bluey_ios/lib/bluey_ios.dart` — wherever they construct these DTOs from the Pigeon types, set the new `handle` field (use a placeholder `0` or `-1` interim if the platform isn't handle-aware yet — this commit is purely additive).

- [ ] **D.2.1** — Add the field.
- [ ] **D.2.2** — Update construction sites with placeholder.
- [ ] **D.2.3** — Run `flutter test` for `bluey_platform_interface` → green.
- [ ] **D.2.4** — Commit:
  ```
  refactor(bluey_platform_interface): add nullable handle field on Platform DTOs (additive, I088 prep)
  ```

## Task D.3: Android — populate `characteristicByHandle` / `descriptorByHandle` at discovery; clear on disconnect / Service Changed

**Files:**
- Modify: `bluey_android/android/src/main/kotlin/com/neutrinographics/bluey/ConnectionManager.kt`
- New: `bluey_android/android/src/test/kotlin/com/neutrinographics/bluey/HandleLookupTest.kt`

- [ ] **D.3.1** — Failing JVM test: `HandleLookupTest.populates handle table at onServicesDiscovered`. Set up a mock `BluetoothGatt` with two services × two characteristics × one descriptor each; fire `onServicesDiscovered`; assert `characteristicByHandle[deviceId]` contains four entries with correct `getInstanceId()` keys.
- [ ] **D.3.2** — Failing test: `HandleLookupTest.clears handle table on STATE_DISCONNECTED`.
- [ ] **D.3.3** — Failing test: `ServiceChangedHandleTest.clears handle table on onServiceChanged before re-discovery`.
- [ ] **D.3.4** — Add fields:
  ```kotlin
  private val characteristicByHandle:
      MutableMap<String, MutableMap<Int, BluetoothGattCharacteristic>> = mutableMapOf()
  private val descriptorByHandle:
      MutableMap<String, MutableMap<Int, BluetoothGattDescriptor>> = mutableMapOf()
  ```
- [ ] **D.3.5** — In `onServicesDiscovered`, after the existing rediscovery flow:
  ```kotlin
  handler.post {
    val charMap = mutableMapOf<Int, BluetoothGattCharacteristic>()
    val descMap = mutableMapOf<Int, BluetoothGattDescriptor>()
    for (service in gatt.services) {
      for (char in service.characteristics) {
        charMap[char.instanceId] = char
        for (desc in char.descriptors) {
          descMap[desc.instanceId] = desc
        }
      }
    }
    characteristicByHandle[deviceId] = charMap
    descriptorByHandle[deviceId] = descMap
  }
  ```
- [ ] **D.3.6** — In `STATE_DISCONNECTED`'s `handler.post` block, add:
  ```kotlin
  characteristicByHandle.remove(deviceId)
  descriptorByHandle.remove(deviceId)
  ```
- [ ] **D.3.7** — In `onServiceChanged`'s `handler.post` block, add the same clears before triggering re-discovery.
- [ ] **D.3.8** — Run JVM tests → green.
- [ ] **D.3.9** — Commit:
  ```
  feat(bluey_android): populate handle table at discovery; clear on disconnect / Service Changed (I088)
  ```

## Task D.4: Android — emit `getInstanceId()` in DTOs from `mapServices` / `mapCharacteristics`

**Files:**
- Modify: `bluey_android/android/src/main/kotlin/com/neutrinographics/bluey/ConnectionManager.kt:879–902`
- Modify: same `mapServices` / `mapCharacteristics` callers in `GattServer.kt` (or wherever the local server's `addService` callback lives).

- [ ] **D.4.1** — In `mapCharacteristics`, set `handle = char.instanceId` in the `CharacteristicDto`.
- [ ] **D.4.2** — In whatever helper maps descriptors, set `handle = desc.instanceId`.
- [ ] **D.4.3** — In `GattServer`'s read/write request handlers, set `characteristicHandle = characteristic.instanceId` on `ReadRequestDto` / `WriteRequestDto`.
- [ ] **D.4.4** — Run JVM tests + Dart tests for `bluey_android` → green.
- [ ] **D.4.5** — Commit:
  ```
  feat(bluey_android): emit getInstanceId() handle in discovery and request DTOs (I088)
  ```

## Task D.5: iOS — mint handles in CentralManager discovery callbacks; clear on disconnect / didModifyServices

**Files:**
- Modify: `bluey_ios/ios/Classes/CentralManagerImpl.swift`
- New: `bluey_ios/ios/Tests/CentralManagerHandleTests.swift`

- [ ] **D.5.1** — Failing XCTest: handles minted at `peripheral(_, didDiscoverCharacteristicsFor:)`; map populated; lookup-by-handle returns the right `CBCharacteristic`.
- [ ] **D.5.2** — Failing XCTest: handles cleared on disconnect; cleared on `peripheral(_, didModifyServices:)`.
- [ ] **D.5.3** — Replace `characteristics: [String: [String: CBCharacteristic]]` with handle-keyed maps:
  ```swift
  private var characteristicByHandle: [String: [Int: CBCharacteristic]] = [:]
  private var descriptorByHandle: [String: [Int: CBDescriptor]] = [:]
  private var nextHandle: [String: Int] = [:]

  private func mintHandle(for deviceId: String) -> Int {
      let h = (nextHandle[deviceId] ?? 0) + 1
      nextHandle[deviceId] = h
      return h
  }
  ```
- [ ] **D.5.4** — In `peripheral(_, didDiscoverCharacteristicsFor:)`, mint handles for each char and store; emit handles in `CharacteristicDto`s sent back to Dart.
- [ ] **D.5.5** — In `peripheral(_, didDiscoverDescriptorsFor:)`, same for descriptors.
- [ ] **D.5.6** — In `centralManager(_, didDisconnectPeripheral:)`, clear all three maps for `[deviceId]`.
- [ ] **D.5.7** — In `peripheral(_, didModifyServices:)`, clear and re-discover.
- [ ] **D.5.8** — Run XCTests → green.
- [ ] **D.5.9** — Commit:
  ```
  feat(bluey_ios): mint handles in CentralManager discovery; clear on disconnect / didModifyServices (I088)
  ```

## Task D.6: iOS — mint handles in PeripheralManager `addService`

**Files:**
- Modify: `bluey_ios/ios/Classes/PeripheralManagerImpl.swift`
- New: `bluey_ios/ios/Tests/PeripheralManagerHandleTests.swift`

- [ ] **D.6.1** — Failing XCTest: server-side `addService` mints handles for each `CBMutableCharacteristic`; handles stored in `[Int: CBMutableCharacteristic]`.
- [ ] **D.6.2** — Replace `characteristics: [String: CBMutableCharacteristic]` with `characteristicByHandle: [Int: CBMutableCharacteristic]` and `private var nextHandle = 1`.
- [ ] **D.6.3** — In `addService`, mint a handle for each char; emit handles in the returned DTO.
- [ ] **D.6.4** — Run XCTests → green.
- [ ] **D.6.5** — Commit:
  ```
  feat(bluey_ios): mint handles in PeripheralManager addService (I088)
  ```

## Task D.7: Domain — `BlueyRemoteCharacteristic` / `BlueyRemoteDescriptor` carry `AttributeHandle`

**Rationale:** The Dart side wraps the wire-level int handle in `AttributeHandle` immediately on receipt. Internal to the GATT op call, unwrap to `int` at the Pigeon-call boundary.

**Files:**
- Modify: `bluey/lib/src/connection/bluey_connection.dart` — `BlueyRemoteService` (L766), `BlueyRemoteCharacteristic` (L818), `BlueyRemoteDescriptor` (L1004).
- Modify: the construction sites where these are built from `PlatformCharacteristic` / `PlatformDescriptor`.

- [ ] **D.7.1** — Add `final AttributeHandle handle` to `BlueyRemoteCharacteristic` and `BlueyRemoteDescriptor`. Construct from `PlatformCharacteristic.handle` (currently nullable; assert non-null at construction with a clear error message until Task D.13 makes it non-nullable).
- [ ] **D.7.2** — Add `handle: AttributeHandle` getter to the abstract `RemoteCharacteristic` and `RemoteDescriptor` interfaces.
- [ ] **D.7.3** — Run `flutter analyze` → expect clean (constructors updated; no callers yet care about the new field).
- [ ] **D.7.4** — Commit:
  ```
  feat(bluey): RemoteCharacteristic and RemoteDescriptor carry AttributeHandle (I088)
  ```

## Task D.8: Switch GATT method calls to send handle alongside UUID (additive interim, both platforms)

**Rationale:** During the additive interim, every GATT method takes both UUID and handle. Native receivers prefer handle when present (non-null) and fall back to UUID otherwise. This lets us migrate platforms one at a time without breaking the suite.

**Files:**
- Modify: `bluey_android/pigeons/messages.dart` — every GATT method gains an `int? characteristicHandle` parameter alongside the existing `String characteristicUuid`. Same for descriptor methods. Regenerate.
- Modify: `bluey_ios/pigeons/messages.dart` — same.
- Modify: `bluey_platform_interface/lib/src/platform_interface.dart` — every GATT method takes both.
- Modify: `bluey/lib/src/connection/bluey_connection.dart` — call sites pass both `_handle.value` and `_uuid.toString()`.
- Modify: `bluey_android/android/src/main/kotlin/com/neutrinographics/bluey/ConnectionManager.kt` — read methods: prefer `characteristicByHandle[deviceId]?[handle]` when non-null; else fall back to `findCharacteristic(gatt, uuid)`.
- Modify: `bluey_ios/ios/Classes/CentralManagerImpl.swift` — same.

- [ ] **D.8.1** — Pigeon edits + regenerate.
- [ ] **D.8.2** — Platform interface signature changes.
- [ ] **D.8.3** — Domain call sites pass both.
- [ ] **D.8.4** — Android receivers prefer handle.
- [ ] **D.8.5** — iOS receivers prefer handle.
- [ ] **D.8.6** — Full suite green on both platforms.
- [ ] **D.8.7** — Commit:
  ```
  refactor(bluey): GATT methods carry both UUID and handle; native sides prefer handle (additive, I088)
  ```

## Task D.9: Tests — handle routing is unambiguous on duplicate-UUID peripherals

**Files:**
- New: `bluey/test/connection/handle_routing_test.dart`
- New: `bluey/test/connection/cccd_routing_test.dart`
- Modify: `bluey/test/fakes/fake_platform.dart` — handle-keyed storage; `handleFor(deviceId, charUuid)` helper.

- [ ] **D.9.1** — Update FakeBlueyPlatform to mint handles at discovery and route by handle.
- [ ] **D.9.2** — Failing test `handle_routing_test.dart`: peripheral exposes service A and service B, both with characteristic charUuid. `connection.services()[0].characteristics(uuid: charUuid).single.read()` and `connection.services()[1].characteristics(uuid: charUuid).single.read()` route to distinct backing values.
- [ ] **D.9.3** — Failing test `cccd_routing_test.dart`: peripheral exposes two notifyable chars, each with CCCD `0x2902`. Subscribing to char A's notifications writes only char A's CCCD; char B's CCCD is untouched.
- [ ] **D.9.4** — Run → green.
- [ ] **D.9.5** — Commit:
  ```
  test(bluey): handle-routing and CCCD-routing tests for duplicate-UUID peripherals (I088, I011)
  ```

## Task D.10: Plural accessors and ambiguity-throw

**Rationale:** Spec Decision 3 — `RemoteService.characteristics({UUID? uuid})`, `RemoteCharacteristic.descriptors({UUID? uuid})`. Singular accessors throw `AmbiguousAttributeException` on duplicates instead of returning first match.

**Files:**
- Modify: `bluey/lib/src/connection/connection.dart` — `RemoteService.characteristics({UUID? uuid})`, `RemoteCharacteristic.descriptors({UUID? uuid})` abstract.
- Modify: `bluey/lib/src/connection/bluey_connection.dart` — implement plural; rewrite singular to throw on >1 match.
- Modify: `bluey/lib/src/exceptions.dart` — add `AmbiguousAttributeException(UUID uuid, int matchCount)`.
- New: `bluey/test/connection/ambiguous_attribute_test.dart`.

- [ ] **D.10.1** — Failing tests:
  - `service.characteristic(charUuid)` on a service with two same-UUID chars throws `AmbiguousAttributeException(charUuid, 2)`.
  - `service.characteristic(charUuid)` with zero matches throws `CharacteristicNotFoundException` (existing).
  - `service.characteristic(charUuid)` with one match returns it (existing).
  - `service.characteristics(uuid: charUuid)` returns the full list when there are duplicates.
  - `service.characteristics()` (no filter) returns all.
  - Same shape for `connection.service(uuid)` and `characteristic.descriptor(uuid)`.
- [ ] **D.10.2** — Add `AmbiguousAttributeException`.
- [ ] **D.10.3** — Add abstract plural accessors.
- [ ] **D.10.4** — Implement plurals on `BlueyRemoteService` / `BlueyRemoteCharacteristic`.
- [ ] **D.10.5** — Rewrite `Connection.service(uuid)`, `RemoteService.characteristic(uuid)`, `RemoteCharacteristic.descriptor(uuid)` to throw on >1 match.
- [ ] **D.10.6** — Run → green.
- [ ] **D.10.7** — Commit:
  ```
  feat(bluey): plural accessors and ambiguity-throw on singular accessors (I088)
  ```

## Task D.11: Service Changed handle invalidation surfaced as `AttributeHandleInvalidatedException`

**Files:**
- Modify: `bluey/lib/src/exceptions.dart` — add `AttributeHandleInvalidatedException`, `AttributeNotFoundException`.
- Modify: `bluey/lib/src/connection/bluey_connection.dart` — `_handleServiceChange` fails in-flight ops with `AttributeHandleInvalidatedException`; clears `_cachedServices`.
- Modify: native sides — when a handle lookup fails (post-Service-Changed), return a `gatt-handle-invalidated` Pigeon error code; domain layer translates it.
- New: integration test in `bluey/test/connection/service_changed_handle_test.dart`.

- [ ] **D.11.1** — Failing test: peripheral fires Service Changed mid-read; pending `Future` fails with `AttributeHandleInvalidatedException`. After Dart re-calls `services()`, new handles work.
- [ ] **D.11.2** — Add the two exceptions.
- [ ] **D.11.3** — Update Android `ConnectionManager.kt`: when `characteristicByHandle[deviceId]?[handle]` returns null *and* the device is connected, return a `gatt-handle-invalidated` Pigeon error.
- [ ] **D.11.4** — Same for iOS `CentralManagerImpl.swift`.
- [ ] **D.11.5** — Domain-side error wrapper translates the Pigeon error code to the typed exception.
- [ ] **D.11.6** — Run → green.
- [ ] **D.11.7** — Commit:
  ```
  feat(bluey): AttributeHandleInvalidatedException surfaces stale-handle ops post-Service-Changed (I088)
  ```

## Task D.12: Migrate FakeBlueyPlatform fully to handle-keyed storage

**Rationale:** D.9 introduced minimal handle support; this task ensures the fake fully matches the production interface (handle-only on the wire). Necessary for D.13 to land cleanly.

**Files:** `bluey/test/fakes/fake_platform.dart`, `bluey/test/fakes/test_helpers.dart`.

- [ ] **D.12.1** — Make handle the primary storage key; UUID is metadata.
- [ ] **D.12.2** — Add `TestHandles` to `test_helpers.dart` with canonical handles paired with `TestUuids`.
- [ ] **D.12.3** — Run full suite → green.
- [ ] **D.12.4** — Commit:
  ```
  test(bluey): FakeBlueyPlatform fully handle-keyed; TestHandles helpers added (I088)
  ```

## Task D.13: Drop UUID parameter from GATT methods (breaking cleanup)

**Files:**
- Modify: `bluey_android/pigeons/messages.dart` — remove the `String characteristicUuid` and `String descriptorUuid` parameters from all GATT methods. Make `int characteristicHandle` non-null. Regenerate.
- Modify: `bluey_ios/pigeons/messages.dart` — same.
- Modify: `bluey_platform_interface/lib/src/platform_interface.dart` — same.
- Modify: `bluey/lib/src/connection/bluey_connection.dart` — drop the UUID arg from every `_platform.read/write/...` call.
- Modify: native receivers — drop the UUID-fallback path; handle is required.
- Modify: `PlatformCharacteristic.handle`, `PlatformDescriptor.handle` non-nullable.

- [ ] **D.13.1** — Pigeon edits + regenerate.
- [ ] **D.13.2** — Platform interface signature drops.
- [ ] **D.13.3** — Domain call-site cleanups.
- [ ] **D.13.4** — Android: remove `findCharacteristic` / `findDescriptor` helpers (now unused). Remove the UUID-fallback branches.
- [ ] **D.13.5** — iOS: same.
- [ ] **D.13.6** — `flutter analyze` clean across all packages.
- [ ] **D.13.7** — Run full suite → green.
- [ ] **D.13.8** — Commit:
  ```
  refactor(pigeons,platform-interface,bluey): drop UUID parameter from GATT methods; handle-only (I088, breaking)
  ```

## Task D.14: Native — remove dead UUID-keyed storage on iOS, dead helpers on Android

**Files:**
- Modify: `bluey_ios/ios/Classes/CentralManagerImpl.swift` — confirm old `characteristics: [String: [String: CBCharacteristic]]` is gone (should already be from D.5).
- Modify: `bluey_ios/ios/Classes/PeripheralManagerImpl.swift` — confirm old UUID-keyed storage gone (D.6).
- Modify: `bluey_android/android/src/main/kotlin/com/neutrinographics/bluey/ConnectionManager.kt` — confirm `findCharacteristic` / `findDescriptor` helpers (L853, L865) are deleted (D.13.4).

- [ ] **D.14.1** — Audit each file for dead UUID-keyed lookup code; delete any stragglers.
- [ ] **D.14.2** — Run all native tests → green.
- [ ] **D.14.3** — Commit (only if anything was missed by earlier tasks):
  ```
  chore(native): remove dead UUID-keyed lookup code (I088)
  ```

## Phase D checkpoint

- [ ] **D.checkpoint.1** — Full Dart test suite green: `bluey`, `bluey_platform_interface`, `bluey_android`, `bluey_ios`.
- [ ] **D.checkpoint.2** — Android JVM tests green: `cd bluey_android/android && ./gradlew test`.
- [ ] **D.checkpoint.3** — iOS XCTest green.
- [ ] **D.checkpoint.4** — `flutter analyze` clean.
- [ ] **D.checkpoint.5** — Manual on real devices (load-bearing):
  - **Multi-service peripheral with duplicate characteristics**: subscribe to one char, verify the other char's notifications are NOT toggled. (Set up via example-app debug mode, or use a real third-party device.)
  - **Service Changed flow**: trigger Service Changed; verify in-flight ops fail with `AttributeHandleInvalidatedException` and re-discovery rebuilds handles.
  - **Existing soak / failure-injection** scenarios pass unchanged.
- [ ] **D.checkpoint.6** — Pause for user review before Phase E.

---

# Phase E — Cleanup, docs, version bump, backlog close-out

## Task E.1: Update CLAUDE.md with handle-identity invariant + connection.android pattern

**Files:** `CLAUDE.md`

- [ ] **E.1.1** — Add a "Handle-based attribute identity" subsection under Architecture: explain the invariant (handles assigned at discovery, invalidated on disconnect / Service Changed, opaque to users), the Dart-side `AttributeHandle` wrapper, and the user-facing `connection.service(uuid).characteristic(uuid)` ergonomic path with throw-on-ambiguous semantics.
- [ ] **E.1.2** — Add a "Platform-tagged Connection extensions" subsection: `connection.android?.bond()` pattern, why iOS gets a typed null.
- [ ] **E.1.3** — Add a "Peer composition" subsection: `Bluey.connect()` returns raw, `Bluey.connectAsPeer()` for peer-protocol path.
- [ ] **E.1.4** — Update the Ubiquitous Language table if any of `AttributeHandle`, `PeerConnection`, `AndroidConnectionExtensions` should be canonicalized.
- [ ] **E.1.5** — Commit:
  ```
  docs(claude): document handle identity, connection.android, PeerConnection
  ```

## Task E.2: Update `ANDROID_BLE_NOTES.md` with handle lifetime invariant

**Files:** `bluey_android/ANDROID_BLE_NOTES.md`

- [ ] **E.2.1** — Append a "Handle lifetime" section: `getInstanceId()` is the canonical identity; populated at `onServicesDiscovered`, cleared on `STATE_DISCONNECTED` and `onServiceChanged`.
- [ ] **E.2.2** — Commit:
  ```
  docs(android): document handle lifetime in ANDROID_BLE_NOTES.md
  ```

## Task E.3: Update `IOS_BLE_NOTES.md` with handle minting

**Files:** `bluey_ios/IOS_BLE_NOTES.md`

- [ ] **E.3.1** — Append a "Handle minting" section: per-device monotonic counter; minted at `didDiscover*` callbacks; cleared on disconnect / `didModifyServices`. Note that CoreBluetooth has no native equivalent of `getInstanceId()` and we fill the gap ourselves.
- [ ] **E.3.2** — Commit:
  ```
  docs(ios): document handle minting in IOS_BLE_NOTES.md
  ```

## Task E.4: Update CHANGELOGs

**Files:** `bluey/CHANGELOG.md`, `bluey_platform_interface/CHANGELOG.md`, `bluey_android/CHANGELOG.md`, `bluey_ios/CHANGELOG.md` (if present).

- [ ] **E.4.1** — Add an entry summarizing the breaking changes:
  - GATT methods now route by opaque handle; UUIDs only used for navigation.
  - `Connection` no longer declares bond / PHY / connection-parameters; use `connection.android?.X()` instead.
  - `Bluey.connect()` returns raw `Connection`. Use `Bluey.connectAsPeer()` for peer-protocol behavior.
  - `Mtu`, `ConnectionInterval`, `PeripheralLatency`, `SupervisionTimeout`, `AttributeHandle` value objects added.
  - Singular `service(uuid)` / `characteristic(uuid)` / `descriptor(uuid)` accessors throw `AmbiguousAttributeException` on duplicate UUIDs (was: silent first-match).
- [ ] **E.4.2** — Commit:
  ```
  docs(changelog): document I088/I089/I300/I301 breaking changes
  ```

## Task E.5: Bump major version across all four packages

**Rationale:** Coordinated major-version bump. Bluey 1.x → 2.0.0 (or whatever is current → next major).

**Files:** `bluey/pubspec.yaml`, `bluey_platform_interface/pubspec.yaml`, `bluey_android/pubspec.yaml`, `bluey_ios/pubspec.yaml`. Cross-package version constraints.

- [ ] **E.5.1** — Read current versions; pick the next major.
- [ ] **E.5.2** — Update each pubspec.
- [ ] **E.5.3** — Update cross-package constraints in dependents (e.g. `bluey: ^2.0.0` in example app).
- [ ] **E.5.4** — Run `flutter pub get` in each package; resolve any constraint mismatches.
- [ ] **E.5.5** — Run full suite once more → green.
- [ ] **E.5.6** — Commit:
  ```
  chore(release): bump to 2.0.0 across all four packages (I088/I089/I300/I301)
  ```

## Task E.6: Close-out backlog entries

**Files:**
- `docs/backlog/I010-characteristic-uuid-lookup-no-service-context.md`
- `docs/backlog/I011-descriptor-uuid-lookup-no-char-context.md`
- `docs/backlog/I016-ios-server-characteristics-uuid-only.md`
- `docs/backlog/I066-connection-platform-specific-methods.md`
- `docs/backlog/I088-pigeon-gatt-schema-rewrite.md`
- `docs/backlog/I089-connection-platform-tagged-extensions.md`
- `docs/backlog/I300-connection-peer-bounded-context.md`
- `docs/backlog/I301-connection-params-mtu-primitive-obsession.md`
- `docs/backlog/README.md`

- [ ] **E.6.1** — For each of the eight entries: change frontmatter `status: open` → `status: fixed`, add `fixed_in: <commit-sha>` (the bundle's last commit SHA from E.5), update `last_verified` to today's date.
- [ ] **E.6.2** — Append a closing prose section: "Fixed in the bundled handle-rewrite (I088 + I089 + I300 + I301). See `docs/superpowers/specs/2026-04-28-pigeon-gatt-handle-rewrite-design.md` for the full design and `docs/superpowers/plans/2026-04-28-pigeon-gatt-handle-rewrite.md` for the execution sequence."
- [ ] **E.6.3** — In `docs/backlog/README.md`:
  - Remove the eight IDs from their respective "Open" sections.
  - Add eight rows to "Fixed — verified in HEAD" with the same SHA.
  - Update Tier 3 in "Suggested order of attack": replace the bundle line with a tilde-strikethrough, citing the closure commit.
- [ ] **E.6.4** — Commit:
  ```
  chore(backlog): mark I010, I011, I016, I066, I088, I089, I300, I301 fixed
  ```

## Task E.7: Final integration sweep

- [ ] **E.7.1** — Full suite green: `cd bluey && flutter test && cd ../bluey_platform_interface && flutter test && cd ../bluey_android && flutter test && cd ../bluey_ios && flutter test`.
- [ ] **E.7.2** — Native tests green: `cd bluey_android/android && ./gradlew test` and the iOS XCTest equivalent.
- [ ] **E.7.3** — `flutter analyze` clean across all four packages.
- [ ] **E.7.4** — Example app builds and runs on both platforms.
- [ ] **E.7.5** — Manual verification scenarios from spec §Testing all pass:
  - Multi-service peripheral with duplicate characteristics — disambiguation works.
  - Service Changed flow — handles clear and rebuild correctly.
  - Existing soak / failure-injection scenarios pass unchanged.
  - iOS connect + bond attempt → `connection.android` is null.
  - Android connect + bond attempt → `connection.android?.bond()` succeeds (or returns I035-Stage-A stub).
  - `connectAsPeer` on non-Bluey peripheral throws `NotABlueyPeerException`.
  - `connect` + `tryUpgrade` on a Bluey peer succeeds.
- [ ] **E.7.6** — Hand back to user; await sign-off before merge.

---

## Branch close-out

- [ ] **F.1** — On user sign-off, fast-forward `main` to `feat/handle-identity-rewrite`:
  ```bash
  git checkout main
  git merge --ff-only feat/handle-identity-rewrite
  ```
  *(Or whatever merge style the user prefers — confirm before pushing.)*
- [ ] **F.2** — Do NOT `git push` without the user's explicit instruction.

---

## Risk notes for the executor

- **Phase D.8 / D.13 ordering trap.** During the additive interim (D.8 through D.12), every Pigeon GATT method takes BOTH UUID and handle. Native receivers prefer handle when non-null. If you accidentally land D.13 (drop UUID) before all native sides are emitting handles correctly, builds break across both platforms simultaneously. Run JVM + XCTest after D.4, D.5, and D.6 to confirm handle emission before crossing into D.13.
- **Service Changed is rare in tests.** D.11 is a load-bearing correctness test but most fakes don't model Service Changed by default. The new test in D.11 may be the first; verify it actually exercises the post-discovery handle map rebuild rather than a no-op.
- **Capabilities matrix asymmetry.** B.2 hinges on capabilities being correct for the test platform. If a test sets up FakeBlueyPlatform with default capabilities (which may default to "Android-flavored" or vice versa), `connection.android` will be unexpectedly non-null/null. Always explicitly set capabilities in extension-related tests.
- **iOS handle counter is per-device.** `nextHandle: [String: Int]` is keyed by deviceId; if a device reconnects, the counter resets to 1 (because the dict entry was cleared on disconnect). That's the correct behavior — the new connection has fresh handles. Don't accidentally make the counter global.
- **Android `getInstanceId()` is implementation-defined.** It returns an Int but the BLE Core spec doesn't mandate uniqueness across services in a peripheral's GATT database (though Android's stack does in practice). The handle table uses it as a key per-device; if a peripheral exposes two services that hash to the same instance ID, the handle table loses one. Our fallback: throw `AttributeNotFoundException` with a message that names the instance ID — this is a "bug in your peripheral or in Android's stack" condition, not a Bluey bug, but be ready to spot it during D.checkpoint.5.
- **Don't `--no-verify` or `--no-edit`.** Per repo conventions, never skip hooks. If a pre-commit hook fails, fix the underlying issue; don't bypass.
- **Don't push.** User handles git pushes themselves (per `~/.claude/.../memory/feedback_no_push.md`).

---

## End-of-plan checklist

- [ ] All eight backlog IDs marked `status: fixed` with the same `fixed_in` SHA.
- [ ] All four `pubspec.yaml` versions bumped to next major.
- [ ] CHANGELOG entries written.
- [ ] CLAUDE.md / ANDROID_BLE_NOTES.md / IOS_BLE_NOTES.md updated.
- [ ] Full suite green; manual verification on real devices passes.
- [ ] Branch awaiting user merge.
