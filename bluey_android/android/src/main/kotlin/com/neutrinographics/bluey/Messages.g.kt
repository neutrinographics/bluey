// Autogenerated from Pigeon (v22.7.4), do not edit directly.
// See also: https://pub.dev/packages/pigeon
@file:Suppress("UNCHECKED_CAST", "ArrayInDataClass")

package com.neutrinographics.bluey

import android.util.Log
import io.flutter.plugin.common.BasicMessageChannel
import io.flutter.plugin.common.BinaryMessenger
import io.flutter.plugin.common.EventChannel
import io.flutter.plugin.common.MessageCodec
import io.flutter.plugin.common.StandardMethodCodec
import io.flutter.plugin.common.StandardMessageCodec
import java.io.ByteArrayOutputStream
import java.nio.ByteBuffer

private fun wrapResult(result: Any?): List<Any?> {
  return listOf(result)
}

private fun wrapError(exception: Throwable): List<Any?> {
  return if (exception is FlutterError) {
    listOf(
      exception.code,
      exception.message,
      exception.details
    )
  } else {
    listOf(
      exception.javaClass.simpleName,
      exception.toString(),
      "Cause: " + exception.cause + ", Stacktrace: " + Log.getStackTraceString(exception)
    )
  }
}

private fun createConnectionError(channelName: String): FlutterError {
  return FlutterError("channel-error",  "Unable to establish connection on channel: '$channelName'.", "")}

/**
 * Error class for passing custom error details to Flutter via a thrown PlatformException.
 * @property code The error code.
 * @property message The error message.
 * @property details The error details. Must be a datatype supported by the api codec.
 */
class FlutterError (
  val code: String,
  override val message: String? = null,
  val details: Any? = null
) : Throwable()

/** Bluetooth adapter state (DTO for platform channel). */
enum class BluetoothStateDto(val raw: Int) {
  UNKNOWN(0),
  UNSUPPORTED(1),
  UNAUTHORIZED(2),
  OFF(3),
  ON(4);

  companion object {
    fun ofRaw(raw: Int): BluetoothStateDto? {
      return values().firstOrNull { it.raw == raw }
    }
  }
}

/** Connection state (DTO for platform channel). */
enum class ConnectionStateDto(val raw: Int) {
  DISCONNECTED(0),
  CONNECTING(1),
  CONNECTED(2),
  DISCONNECTING(3);

  companion object {
    fun ofRaw(raw: Int): ConnectionStateDto? {
      return values().firstOrNull { it.raw == raw }
    }
  }
}

/**
 * Scan configuration (DTO for platform channel).
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class ScanConfigDto (
  /** Service UUIDs to filter by. */
  val serviceUuids: List<String>,
  /** Timeout in milliseconds (null for no timeout). */
  val timeoutMs: Long? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): ScanConfigDto {
      val serviceUuids = pigeonVar_list[0] as List<String>
      val timeoutMs = pigeonVar_list[1] as Long?
      return ScanConfigDto(serviceUuids, timeoutMs)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      serviceUuids,
      timeoutMs,
    )
  }
}

/**
 * Connect configuration (DTO for platform channel).
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class ConnectConfigDto (
  /** Connection timeout in milliseconds. */
  val timeoutMs: Long? = null,
  /** Requested MTU. */
  val mtu: Long? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): ConnectConfigDto {
      val timeoutMs = pigeonVar_list[0] as Long?
      val mtu = pigeonVar_list[1] as Long?
      return ConnectConfigDto(timeoutMs, mtu)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      timeoutMs,
      mtu,
    )
  }
}

/**
 * A discovered device (DTO for platform channel).
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class DeviceDto (
  /** Device ID. */
  val id: String,
  /** Device name. */
  val name: String? = null,
  /** RSSI in dBm. */
  val rssi: Long,
  /** Service UUIDs. */
  val serviceUuids: List<String>,
  /** Manufacturer data company ID. */
  val manufacturerDataCompanyId: Long? = null,
  /** Manufacturer data bytes. */
  val manufacturerData: List<Long>? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): DeviceDto {
      val id = pigeonVar_list[0] as String
      val name = pigeonVar_list[1] as String?
      val rssi = pigeonVar_list[2] as Long
      val serviceUuids = pigeonVar_list[3] as List<String>
      val manufacturerDataCompanyId = pigeonVar_list[4] as Long?
      val manufacturerData = pigeonVar_list[5] as List<Long>?
      return DeviceDto(id, name, rssi, serviceUuids, manufacturerDataCompanyId, manufacturerData)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      id,
      name,
      rssi,
      serviceUuids,
      manufacturerDataCompanyId,
      manufacturerData,
    )
  }
}

/**
 * Connection state event (DTO for platform channel).
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class ConnectionStateEventDto (
  val deviceId: String,
  val state: ConnectionStateDto
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): ConnectionStateEventDto {
      val deviceId = pigeonVar_list[0] as String
      val state = pigeonVar_list[1] as ConnectionStateDto
      return ConnectionStateEventDto(deviceId, state)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      deviceId,
      state,
    )
  }
}
private open class MessagesPigeonCodec : StandardMessageCodec() {
  override fun readValueOfType(type: Byte, buffer: ByteBuffer): Any? {
    return when (type) {
      129.toByte() -> {
        return (readValue(buffer) as Long?)?.let {
          BluetoothStateDto.ofRaw(it.toInt())
        }
      }
      130.toByte() -> {
        return (readValue(buffer) as Long?)?.let {
          ConnectionStateDto.ofRaw(it.toInt())
        }
      }
      131.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          ScanConfigDto.fromList(it)
        }
      }
      132.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          ConnectConfigDto.fromList(it)
        }
      }
      133.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          DeviceDto.fromList(it)
        }
      }
      134.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          ConnectionStateEventDto.fromList(it)
        }
      }
      else -> super.readValueOfType(type, buffer)
    }
  }
  override fun writeValue(stream: ByteArrayOutputStream, value: Any?)   {
    when (value) {
      is BluetoothStateDto -> {
        stream.write(129)
        writeValue(stream, value.raw)
      }
      is ConnectionStateDto -> {
        stream.write(130)
        writeValue(stream, value.raw)
      }
      is ScanConfigDto -> {
        stream.write(131)
        writeValue(stream, value.toList())
      }
      is ConnectConfigDto -> {
        stream.write(132)
        writeValue(stream, value.toList())
      }
      is DeviceDto -> {
        stream.write(133)
        writeValue(stream, value.toList())
      }
      is ConnectionStateEventDto -> {
        stream.write(134)
        writeValue(stream, value.toList())
      }
      else -> super.writeValue(stream, value)
    }
  }
}


/**
 * Host API - called from Dart to platform.
 *
 * Generated interface from Pigeon that represents a handler of messages from Flutter.
 */
interface BlueyHostApi {
  /** Get current Bluetooth state. */
  fun getState(callback: (Result<BluetoothStateDto>) -> Unit)
  /**
   * Request to enable Bluetooth.
   * Returns true if enabled, false if user declined.
   */
  fun requestEnable(callback: (Result<Boolean>) -> Unit)
  /** Open system Bluetooth settings. */
  fun openSettings(callback: (Result<Unit>) -> Unit)
  /** Start scanning. */
  fun startScan(config: ScanConfigDto, callback: (Result<Unit>) -> Unit)
  /** Stop scanning. */
  fun stopScan(callback: (Result<Unit>) -> Unit)
  /**
   * Connect to a device.
   * Returns connection handle.
   */
  fun connect(deviceId: String, config: ConnectConfigDto, callback: (Result<String>) -> Unit)
  /** Disconnect from a device. */
  fun disconnect(deviceId: String, callback: (Result<Unit>) -> Unit)

  companion object {
    /** The codec used by BlueyHostApi. */
    val codec: MessageCodec<Any?> by lazy {
      MessagesPigeonCodec()
    }
    /** Sets up an instance of `BlueyHostApi` to handle messages through the `binaryMessenger`. */
    @JvmOverloads
    fun setUp(binaryMessenger: BinaryMessenger, api: BlueyHostApi?, messageChannelSuffix: String = "") {
      val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.bluey_android.BlueyHostApi.getState$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.getState{ result: Result<BluetoothStateDto> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.bluey_android.BlueyHostApi.requestEnable$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.requestEnable{ result: Result<Boolean> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.bluey_android.BlueyHostApi.openSettings$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.openSettings{ result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(wrapError(error))
              } else {
                reply.reply(wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.bluey_android.BlueyHostApi.startScan$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val configArg = args[0] as ScanConfigDto
            api.startScan(configArg) { result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(wrapError(error))
              } else {
                reply.reply(wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.bluey_android.BlueyHostApi.stopScan$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.stopScan{ result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(wrapError(error))
              } else {
                reply.reply(wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.bluey_android.BlueyHostApi.connect$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val deviceIdArg = args[0] as String
            val configArg = args[1] as ConnectConfigDto
            api.connect(deviceIdArg, configArg) { result: Result<String> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.bluey_android.BlueyHostApi.disconnect$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val deviceIdArg = args[0] as String
            api.disconnect(deviceIdArg) { result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(wrapError(error))
              } else {
                reply.reply(wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
    }
  }
}
/**
 * Flutter API - called from platform to Dart.
 *
 * Generated class from Pigeon that represents Flutter messages that can be called from Kotlin.
 */
class BlueyFlutterApi(private val binaryMessenger: BinaryMessenger, private val messageChannelSuffix: String = "") {
  companion object {
    /** The codec used by BlueyFlutterApi. */
    val codec: MessageCodec<Any?> by lazy {
      MessagesPigeonCodec()
    }
  }
  /** Bluetooth state changed. */
  fun onStateChanged(stateArg: BluetoothStateDto, callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.bluey_android.BlueyFlutterApi.onStateChanged$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(stateArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(createConnectionError(channelName)))
      } 
    }
  }
  /** Device discovered during scan. */
  fun onDeviceDiscovered(deviceArg: DeviceDto, callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.bluey_android.BlueyFlutterApi.onDeviceDiscovered$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(deviceArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(createConnectionError(channelName)))
      } 
    }
  }
  /** Scan completed or stopped. */
  fun onScanComplete(callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.bluey_android.BlueyFlutterApi.onScanComplete$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(null) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(createConnectionError(channelName)))
      } 
    }
  }
  /** Connection state changed. */
  fun onConnectionStateChanged(eventArg: ConnectionStateEventDto, callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.bluey_android.BlueyFlutterApi.onConnectionStateChanged$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(eventArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(createConnectionError(channelName)))
      } 
    }
  }
}
